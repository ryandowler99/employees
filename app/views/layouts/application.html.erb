<!DOCTYPE html>
<html>
  <head>
    <title>EmployeeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
   <header class="p-3 bg-dark text-white">
    <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-decoration-none homeBtn">
        Home
        </a>

        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
          <li><%# link_to "Home", home_path, class:"nav-link px-2 text-secondary" %></li>
          <li><%= link_to "New Employee", new_employee_path, class:"nav-link px-2 text-white" %></li>
          <li><%= link_to "All Employees", employees_path, class:"nav-link px-2 text-white" %></li>
          <li><%= link_to "All Holidays", holidays_path, class:"nav-link px-2 text-white" %></li>
          <li><%= link_to "New holiday", new_holiday_path, class:"nav-link px-2 text-white" %></li>
        </ul>

        <form class="col-12 col-lg-auto mb-3 mb-lg-0 me-lg-3" role="search">
          <input type="search" class="form-control form-control-dark text-white bg-dark" placeholder="Search..." aria-label="Search">
        </form>

        <div class="text-end">
        <% if !user_signed_in? %>
        <%= link_to "Sign up", new_user_registration_path, class:"btn btn-warning" %>
        <%= link_to "Sign in", new_user_session_path, class:"btn btn-outline-light me-2" %>
        <% else %>
           <%= button_to("Log out", destroy_user_session_path, data: { turbo: false, turbo_method: :delete }, method: :delete, class:'btn btn-outline-light me-2') %>
        <% end %>
        </div>
      </div>
    </div>
  </header>
  <div class="container">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
    </div>
  </body>
</html>
